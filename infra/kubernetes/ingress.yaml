apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: casa-church-api-ingress
  namespace: casa-church
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: casa-church-api-service
                port:
                  number: 80
